name: Build Return Matrix

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *" # daily 6am UTC

# You do NOT need write perms just to upload artifacts.
# Keep PR perms only if you want the optional PR step.
permissions:
  contents: read
  pull-requests: write

concurrency:
  group: return-matrix
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pandas numpy yfinance

      - name: Run return matrix script
        run: |
          mkdir -p data
          python scripts/build_return_matrix.py

      - name: Build clusters
        run: python scripts/build_clusters.py

      - name: Verify outputs exist (and show files)
        run: |
          echo "PWD:"
          pwd
          echo "Repo root:"
          ls -lah
          echo "Listing data/:"
          ls -lah data
          test -f data/daily_returns.csv
          test -f data/correlation_matrix.csv
          echo "✅ Outputs exist."

      - name: Upload outputs as artifacts (guaranteed)
        uses: actions/upload-artifact@v4
        with:
          name: return-matrix
          path: |
            data/daily_returns.csv
            data/correlation_matrix.csv
          if-no-files-found: error

      # Optional: try PR update, but don't fail the build if policy blocks it
      - name: Create/Update PR (optional)
        continue-on-error: true
        uses: peter-evans/create-pull-request@v6
        with:
          branch: auto/return-matrix
          base: main
          title: "Auto update return matrix"
          commit-message: "Auto update return matrix"
          body: "Automated update of daily_returns.csv and correlation_matrix.csv"
          add-paths: |
            data/daily_returns.csv
            data/correlation_matrix.csv
            data/clusters.json
          delete-branch: false
